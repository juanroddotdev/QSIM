# Mock Mode Configuration
# Set to 'true' or '1' to enable mock mode (no Shopify credentials needed)
# When OAuth token is available, mock mode is automatically disabled
MOCK_MODE=true

# Shopify Configuration
SHOP_NAME=your-shop-name
CLIENT_ID=your-shopify-app-client-id
CLIENT_SECRET=your-shopify-app-client-secret

# For direct API access (alternative to OAuth - not needed if using OAuth)
API_KEY=your-admin-api-access-token-here

# Webhook Security (use Client Secret as webhook secret)
WEBHOOK_SECRET=your-client-secret-here

# Server Configuration
PORT=3000
NODE_ENV=development

# OAuth Redirect URI (optional - defaults to http://localhost:PORT/auth/callback)
# Use this if you're using ngrok or a different URL
# OAUTH_REDIRECT_URI=https://your-ngrok-url.ngrok.io/auth/callback
