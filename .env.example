# Mock Mode Configuration
# Set to 'true' or '1' to enable mock mode (no Shopify credentials needed)
MOCK_MODE=true

# Shopify Configuration (only required if MOCK_MODE is false)
SHOP_NAME=your-shop-name
API_KEY=your-shopify-private-app-api-key

# Webhook Security (only required if MOCK_MODE is false)
WEBHOOK_SECRET=your-webhook-shared-secret

# Server Configuration
PORT=3000
NODE_ENV=development
